# Auth Module
This directory will contain the authentication logic extracted from `app.js`.

**Suggested Structure:**
- `auth.js` - Contains `handleLogin`, `logoutUser`, `checkAuth`
- `session.js` - Contains session token management
