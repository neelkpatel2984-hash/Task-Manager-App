# Firebase Module
This directory will contain all Firebase integrations.

**Suggested Structure:**
- `config.js` - Firebase initialization (migrate `js/firebase-config.js` here)
- `db.js` - Wrapper functions for Firestore CRUD operations
